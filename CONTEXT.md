# ChatLuna Studio

本上下文描述在 Koishi 控制台里观察 ChatLuna 真实模型请求、并直接编辑其预设文件的工作室。

## Language

**模型请求记录**:
一次 ChatLuna 对模型服务的 HTTP 调用留下的完整证据：地址、请求头、请求体、响应原文、状态、耗时、用量与错误。记录在 ChatLuna 的公共 fetch 边界上采集，因此与哪个模型服务商、哪种协议无关。
_Avoid_: 日志, 调用日志, 请求日志, 埋点

**归属**:
一条模型请求记录与触发它的那一轮真实对话之间的联系。归属由 ChatLuna 的对话生命周期事件判定，只在恰好一轮对话在跑时成立；判不出来的记录记为未归属，而不是猜一个会话。
_Avoid_: 关联, 绑定, 溯源, 匹配

**会话实体**:
一条记录带的会话现场：平台、机器人账号与昵称、会话标识与名称、会话类型、群号、发言者账号与昵称。名称随请求一起快照，读取时不再回查 Koishi 运行时。
_Avoid_: 会话信息, 上下文, 元数据, 目录项

**交互**:
一轮对话里的全部模型请求共享的标识。工具调用循环会让一轮对话发出多次请求，交互把它们聚成一组。
_Avoid_: 会话轮, 批次, 请求组, 事务

**范围**:
列表读取时的归属过滤：全部记录、已归属会话、未归属。它是同一个记录库上的过滤条件，不是另一个存储。
_Avoid_: 空间, 记录域, 分类, 库

**记录库**:
持有全部模型请求记录的服务端组件：单调序号、游标分页、条数与字节上限、超限从最旧开始回收。它有内存与 Koishi Database 两种适配器，两者给出等价结果。
_Avoid_: 存储, 仓库, 缓存, 表

**模型证据**:
从请求体与响应原文投影出的协议无关结构：请求消息、工具定义、工具调用、工具结果、响应消息与变量展开。它是轨迹、对话视图、分析视图与预设定位共同的唯一读法。
_Avoid_: 解析结果, 结构化数据, AST

**证据身份**:
一条模型证据在记录内的稳定标识（例如 `req:message:messages.0`），跨视图指认同一段内容，也是预设表达式定位的落点。
_Avoid_: 索引, 路径, 坐标

**轨迹**:
按模型证据派生的账本视图：单次请求或同一会话内多次请求按时间铺开的行，配一条请求组成分段。它在读取时派生，不落库。
_Avoid_: 时间线, 流程图, 调用链

**预设文档**:
ChatLuna 主功能或 chatluna-character 磁盘上的一份 YAML 预设，本插件按展示身份（而不是文件名）指认它，并记录修订用于乐观并发。
_Avoid_: 配置, 人格文件, 角色卡

**表达式**:
预设模板里的一个占位段（`{name}`）。可点击的表达式能在真实模型请求里定位到它展开后的精确文本范围；控制标签与整段模板字段不属于可定位的表达式。
_Avoid_: 变量, 插值, 占位符, 模板标签

**证据定位**:
从预设表达式出发，找到最近一次用到该预设当前模板的有归属成功请求，并在它的模型证据里标出展开范围。找不到、坐标已变或存在多个可能范围时明确失败。
_Avoid_: 搜索, 跳转, 高亮

**证据导航**:
跨页面打开某条模型请求、按需定位到某条证据、并在返回时把预设页面恢复回原处的那一次意图。它是客户端瞬时状态，不落库。
_Avoid_: 路由, 历史, 面包屑

**工作室 shell**:
页面组件之上的一层，持有跨区域规则：切视图、预设脏值守卫、错误闸门与证据导航接线。页面只做布局与事件绑定。
_Avoid_: store, 全局状态, 容器组件
