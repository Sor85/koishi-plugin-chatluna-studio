# 克隆并原样保存模型响应体

ChatLuna requester 会独占读取 undici `Response` 的正文流，模型响应采集必须在公共 fetch 包装层返回响应前同步调用 `clone()`，并立即异步消费克隆流，不能读取或等待原始响应。记录只保存完整原始响应文本及由采集时判断的 JSON、文本或 SSE 格式，不保存 HTTP headers；JSON 解析和 SSE 事件数组在读取时由共享模型证据投影派生，避免公共 fetch 层猜测不同供应商的增量聚合语义，也保留失败、半结构化和流式响应的原始证据。
